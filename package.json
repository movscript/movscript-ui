{
  "name": "movscript-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared design tokens and React UI primitives for MovScript products.",
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "tsup": "^8.5.0",
    "typescript": "^5.8.0"
  }
}

